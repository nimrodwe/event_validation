[pytest]
testpaths = tests
pythonpath = .
addopts = --alluredir=out/allure-results --clean-alluredir --allure-no-capture
markers =
    negative: intentional failure / edge-reject case (dashboard yellow (N) tag)